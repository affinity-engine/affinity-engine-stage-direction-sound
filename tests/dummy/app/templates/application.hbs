{{#affinity-engine engineId="sound-direction" config=config fixtures=fixtures config=config isFocused=true as |engine|}}
  {{#if engine.isLoaded}}
    {{affinity-engine-stage engineId=engine.engineId initialScene="test-scenarios/stage/directions/sound"}}
  {{else}}
    {{#affinity-engine-curtain engineId=engine.engineId filesToPreload=(hash sounds="src") completePreload=engine.completePreload as |curtain|}}
      {{curtain.progress}}
    {{/affinity-engine-curtain}}
  {{/if}}
{{/affinity-engine}}
