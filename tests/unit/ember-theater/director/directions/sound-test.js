import Sound from 'ember-theater-director-sound/ember-theater/director/directions/sound';
import { module, test } from 'qunit';

module('Unit | Ember Theater Direction | sound');

// Replace this with your real tests.
test('it works', function(assert) {
  var sound = Sound.create();
  assert.ok(sound);
});
